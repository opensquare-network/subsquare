import ErrorMessage from "../styled/errorMessage";

export default function LoginAddressNotDetectedMessage() {
  return (
    <ErrorMessage>
      Address not detected, please create an available address.
    </ErrorMessage>
  );
}
