import styled from "styled-components";

export const Button = styled.div.attrs({ role: "button" })`
  cursor: pointer;
  font-weight: 500;
  font-size: 12px;
  line-height: 16px;
  color: var(--theme500);
`;
