export default function CoretimeActiveSaleSubscriber({ children }) {
  // todo: 1. subscribe current sale from graphql API
  // todo: 2. subscribe on chain sale info and update info field
  // const [, setActiveSale] = useCoretimeActiveSale();
  return children;
}
